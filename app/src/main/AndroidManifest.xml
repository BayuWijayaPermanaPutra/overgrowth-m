<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.overgrowth">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- permission required to use Alarm Manager -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM"/>
    <!-- Register the Alarm Receiver -->
    <receiver android:name="com.example.alarmmanagernotifcation.AlarmReceiver"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MulaiTanamPilihanActivity" />
        <activity android:name=".MulaiTanamRekomendasiActivity" />
        <activity android:name=".PilihKategoriActivity" />
        <activity android:name=".ListTanamanActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".DetailTanamanPilihActivity" />
        <activity android:name=".DetailDeskripsiTanamanActivity" />
        <activity android:name=".DetailTanamanUserBelumTanamActivity" />
        <activity android:name=".DetailCaraMenanamActivity" />
        <activity android:name=".DetailTanamanUserSudahTanamActivity" />
        <activity android:name=".PengaturanActivity"></activity>

        <receiver android:name=".utility.AlarmReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <!-- Will not be called unless the application explicitly enables it -->

    </application>

</manifest>